# Secrets locais (IA etc.) — vão no Netlify Drop, não no GitHub
nossacasa/config.secrets.js
*.secrets.js
.env
.env.*

# Extratos/dumps com dados de clientes do Fiado — nunca subir
fiado/extrato-*.txt
diario_*.txt

# Backup do Fiado: config tem senha admin; dumps têm dados de clientes
fiado/backup/backup-config.json
fiado/backup/dumps/
